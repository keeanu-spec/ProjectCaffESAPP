// services/api.js
// Archivo placeholder para la conexión con el backend
// Tu compañero completará esto con la lógica real

export const authService = {
    login: async (email, password) => {
        // Placeholder - será completado más adelante
        try {
            // const response = await axios.post('http://localhost:5000/login', { email, password });
            // return response.data;
            throw new Error('Servicio de autenticación no configurado aún');
        } catch (error) {
            throw error;
        }
    }
};
